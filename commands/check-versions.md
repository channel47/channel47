---
name: check-versions
description: Check MCP server package versions against latest available on npm
allowed-tools: Bash
---

# MCP Version Checker

Run `bash check-versions.sh` from the repo root to compare pinned MCP package versions against npm latest.
