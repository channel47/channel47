# Plugin Updates

When modifying any plugin in `plugins/`, update version numbers in:

1. `plugins/{plugin}/.claude-plugin/plugin.json`
2. `plugins/{plugin}/package.json`
3. `.claude-plugin/marketplace.json`

Add a changelog entry in `plugins/{plugin}/CHANGELOG.md` for non-trivial changes.
