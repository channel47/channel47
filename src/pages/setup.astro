---
/**
 * Setup Guide
 *
 * Free, public, SEO-targeted.
 * Instructions for connecting the hosted MCP server.
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/Button.astro';
---

<BaseLayout
  title="Setup Guide — Connect Jig to Your AI Tools"
  description="Get started with Jig in under a minute. Connect the MCP server to Claude Desktop, Cursor, or any compatible tool."
>
  <Header />

  <main class="setup-page">
    <article class="setup-content">
      <header class="setup-header">
        <h1>Setup Guide</h1>
        <p class="setup-intro">
          Get Jig connected to your AI tools in under a minute. No installation
          required—just add a URL.
        </p>
      </header>

      <!-- What You'll Need -->
      <section class="setup-section">
        <h2>What you'll need</h2>
        <ul class="requirements-list">
          <li>
            <strong>An MCP-compatible AI tool</strong> — Claude Desktop, Cursor,
            Windsurf, or any tool that supports the Model Context Protocol.
          </li>
          <li>
            <strong>30 seconds</strong> — That's genuinely all the setup takes.
          </li>
        </ul>
      </section>

      <!-- Connect the MCP -->
      <section class="setup-section">
        <h2>Connect the hosted MCP</h2>
        <p>
          Jig runs as a hosted MCP server. You don't install anything—you just
          point your AI tool to our server URL and the skills become available
          automatically.
        </p>

        <div class="setup-step">
          <h3>For Claude Desktop</h3>
          <ol>
            <li>Open Claude Desktop and go to <strong>Settings</strong></li>
            <li>Navigate to the <strong>Developer</strong> section</li>
            <li>Click <strong>Edit Config</strong> to open your configuration file</li>
            <li>Add the Jig server to your <code>mcpServers</code> configuration:</li>
          </ol>
          <pre class="code-block"><code>{`{
  "mcpServers": {
    "jig": {
      "url": "https://mcp.jig.tools/sse"
    }
  }
}`}</code></pre>
          <ol start="5">
            <li>Save the file and restart Claude Desktop</li>
          </ol>
        </div>

        <div class="setup-step">
          <h3>For Cursor</h3>
          <ol>
            <li>Open Cursor Settings (<code>Cmd/Ctrl + ,</code>)</li>
            <li>Search for "MCP" or navigate to <strong>Features → MCP Servers</strong></li>
            <li>Click <strong>Add Server</strong></li>
            <li>Enter the server URL: <code>https://mcp.jig.tools/sse</code></li>
            <li>Save and restart Cursor</li>
          </ol>
        </div>

        <div class="setup-step">
          <h3>For other MCP tools</h3>
          <p>
            The connection process is similar for any MCP-compatible tool. Add this
            URL wherever your tool accepts MCP server configurations:
          </p>
          <div class="url-display">
            <code>https://mcp.jig.tools/sse</code>
            <button class="copy-button" data-copy="https://mcp.jig.tools/sse">
              Copy
            </button>
          </div>
        </div>
      </section>

      <!-- Verify It Works -->
      <section class="setup-section">
        <h2>Verify it works</h2>
        <p>
          Once connected, test that the skills are loading correctly:
        </p>
        <ol>
          <li>Start a new conversation with your AI tool</li>
          <li>
            Ask: <em>"What Jig skills do you have access to?"</em>
          </li>
          <li>
            Your AI should list available skill categories. If you have purchased
            skills, they'll appear here.
          </li>
        </ol>
        <p>
          If the connection isn't working, double-check the URL and make sure
          you've restarted your AI tool after adding the configuration.
        </p>
      </section>

      <!-- What's Next -->
      <section class="setup-section section--cta">
        <h2>What's next?</h2>
        <p>
          You're connected to Jig's free skill infrastructure. To unlock the full
          Google Ads skill package—47 GAQL queries, audit workflows, and failure
          pattern checklists—grab the complete package:
        </p>
        <div class="cta-buttons">
          <Button href="/google-ads-skills" variant="primary" size="lg">
            Get Google Ads Skills — $199
          </Button>
        </div>
        <p class="cta-note">
          One-time purchase. Lifetime updates. 14-day money-back guarantee.
        </p>
      </section>
    </article>
  </main>

  <Footer />
</BaseLayout>

<script>
  // Copy button functionality
  document.querySelectorAll('.copy-button').forEach(button => {
    button.addEventListener('click', async () => {
      const text = button.getAttribute('data-copy');
      if (text) {
        await navigator.clipboard.writeText(text);
        const originalText = button.textContent;
        button.textContent = 'Copied!';
        setTimeout(() => {
          button.textContent = originalText;
        }, 2000);
      }
    });
  });
</script>

<style>
  .setup-page {
    padding: var(--space-8) var(--space-4);
    padding-top: var(--space-12);
  }

  .setup-content {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .setup-header {
    margin-bottom: var(--space-12);
  }

  .setup-header h1 {
    font-size: var(--text-4xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
  }

  .setup-intro {
    font-size: var(--text-xl);
    color: var(--color-fg-secondary);
    margin: 0;
  }

  .setup-section {
    margin-bottom: var(--space-12);
  }

  .setup-section h2 {
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-4);
  }

  .setup-section h3 {
    font-size: var(--text-lg);
    font-weight: var(--font-semibold);
    margin-bottom: var(--space-3);
    margin-top: var(--space-6);
  }

  .setup-section p {
    margin-bottom: var(--space-4);
  }

  .requirements-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .requirements-list li {
    position: relative;
    padding-left: var(--space-6);
    margin-bottom: var(--space-4);
  }

  .requirements-list li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--color-accent);
  }

  .setup-step {
    margin-bottom: var(--space-8);
    padding: var(--space-6);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .setup-step h3 {
    margin-top: 0;
  }

  .setup-step ol {
    padding-left: var(--space-5);
    margin-bottom: var(--space-4);
  }

  .setup-step ol li {
    margin-bottom: var(--space-2);
  }

  .code-block {
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    padding: var(--space-4);
    overflow-x: auto;
    margin: var(--space-4) 0;
  }

  .code-block code {
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-fg);
    background: none;
    padding: 0;
  }

  .url-display {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    background-color: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    margin-top: var(--space-3);
  }

  .url-display code {
    flex: 1;
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    color: var(--color-accent);
    background: none;
    padding: 0;
  }

  .copy-button {
    font-family: var(--font-sans);
    font-size: var(--text-sm);
    font-weight: var(--font-medium);
    padding: var(--space-2) var(--space-3);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
    color: var(--color-fg-secondary);
    cursor: pointer;
    transition: all var(--duration-fast) var(--ease-out);
  }

  .copy-button:hover {
    background-color: var(--color-bg-hover);
    color: var(--color-fg);
  }

  .section--cta {
    padding: var(--space-8);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .section--cta h2 {
    text-align: center;
  }

  .section--cta p {
    max-width: 50ch;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-buttons {
    margin: var(--space-6) 0;
  }

  .cta-note {
    font-size: var(--text-sm);
    color: var(--color-fg-muted);
    margin-bottom: 0;
  }
</style>
