---
/**
 * Homepage
 *
 * Introduce concept, build intrigue, route traffic.
 * Direct, confident tone. Peer to peer.
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Button from '../components/Button.astro';
import EmailSignup from '../components/EmailSignup.astro';
---

<BaseLayout
  title="Jig — Your tools are capable. Now make them competent."
  description="Jig is packaged expertise—skill files that teach Claude, Cursor, and other AI tools how to actually do the job."
>
  <Header />

  <main class="main">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero__content">
        <h1 class="hero__title" data-animate="fade-up">Your tools are capable. Now make them competent.</h1>
        <p class="hero__subtitle" data-animate="fade-up">
          Jig is packaged expertise—skill files that teach Claude, Cursor, and other AI tools how to actually do the job.
        </p>
      </div>
    </section>

    <!-- Body Section -->
    <section class="body-section">
      <div class="body-content" data-animate="fade-up">
        <p class="lead">The API is the easy part.</p>
        <p>
          Connecting your tools to Google Ads or a codebase or a database—that's solved. Plenty of MCP servers. Plenty of tutorials.
        </p>
        <p>But capability isn't competence.</p>
        <p>
          Knowing how to query an account isn't the same as knowing what to look for. Having access isn't the same as having judgment.
        </p>
        <p class="emphasis">That's where Jig comes in.</p>
        <p>
          Skill files that encode real expertise—workflows, checklists, decision logic—so your tools perform like someone who's done this a thousand times.
        </p>
      </div>
    </section>

    <!-- Paths Section -->
    <section class="paths-section">
      <div class="paths-grid" data-animate-stagger>
        <div class="path-card" data-animate="fade-up">
          <h2 class="path-card__title">Google Ads Skills</h2>
          <p class="path-card__description">Ten years of media buying. One install. $199.</p>
          <Button href="/google-ads-skills" variant="primary">See what's included</Button>
        </div>
        <div class="path-card" data-animate="fade-up">
          <h2 class="path-card__title">Setup Guide</h2>
          <p class="path-card__description">Connect the free hosted MCP in five minutes.</p>
          <Button href="/setup" variant="outline">Get started</Button>
        </div>
      </div>
    </section>

    <!-- Email Signup -->
    <section class="signup-section">
      <div class="signup-wrapper" data-animate="fade-up">
        <EmailSignup
          title="New skills in the works."
          description="Leave your email. We'll let you know."
          buttonText="Subscribe"
          variant="card"
        />
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  .main {
    display: flex;
    flex-direction: column;
  }

  /* Hero Section */
  .hero {
    padding: var(--space-16) var(--space-4) var(--space-10);
    text-align: center;
  }

  @media (min-width: 768px) {
    .hero {
      padding: var(--space-24) var(--space-8) var(--space-12);
    }
  }

  .hero__content {
    max-width: var(--container-md);
    margin: 0 auto;
  }

  .hero__title {
    font-size: var(--text-3xl);
    font-weight: var(--font-black);
    letter-spacing: var(--tracking-tighter);
    margin-bottom: var(--space-5);
    color: var(--color-fg);
    line-height: 1.1;
  }

  @media (min-width: 480px) {
    .hero__title {
      font-size: var(--text-4xl);
    }
  }

  @media (min-width: 768px) {
    .hero__title {
      font-size: var(--text-5xl);
      margin-bottom: var(--space-6);
      letter-spacing: var(--tracking-tightest);
    }
  }

  .hero__subtitle {
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
    line-height: var(--leading-relaxed);
    max-width: 50ch;
    margin: 0 auto;
  }

  @media (min-width: 768px) {
    .hero__subtitle {
      font-size: var(--text-lg);
      max-width: 55ch;
    }
  }

  /* Body Section */
  .body-section {
    padding: var(--space-8) var(--space-4);
    background-color: var(--color-bg-elevated);
  }

  @media (min-width: 768px) {
    .body-section {
      padding: var(--space-16) var(--space-8);
    }
  }

  .body-content {
    max-width: var(--content-width);
    margin: 0 auto;
  }

  .body-content p {
    margin-bottom: var(--space-5);
    font-size: var(--text-base);
    color: var(--color-fg-secondary);
  }

  @media (min-width: 768px) {
    .body-content p {
      font-size: var(--text-lg);
    }
  }

  .body-content .lead {
    font-weight: var(--font-semibold);
    color: var(--color-fg);
    font-size: var(--text-lg);
  }

  @media (min-width: 768px) {
    .body-content .lead {
      font-size: var(--text-xl);
    }
  }

  .body-content .emphasis {
    font-weight: var(--font-semibold);
    color: var(--color-fg);
  }

  /* Paths Section */
  .paths-section {
    padding: var(--space-12) var(--space-4);
  }

  @media (min-width: 768px) {
    .paths-section {
      padding: var(--space-16) var(--space-8);
    }
  }

  .paths-grid {
    display: grid;
    gap: var(--space-4);
    max-width: var(--container-lg);
    margin: 0 auto;
  }

  @media (min-width: 640px) {
    .paths-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-6);
    }
  }

  .path-card {
    padding: var(--space-6);
    background-color: var(--color-bg-elevated);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    text-align: center;
  }

  @media (min-width: 768px) {
    .path-card {
      padding: var(--space-8);
    }
  }

  .path-card__title {
    font-size: var(--text-xl);
    font-weight: var(--font-bold);
    margin-bottom: var(--space-2);
    color: var(--color-fg);
  }

  @media (min-width: 768px) {
    .path-card__title {
      font-size: var(--text-2xl);
    }
  }

  .path-card__description {
    font-size: var(--text-sm);
    color: var(--color-fg-secondary);
    margin-bottom: var(--space-6);
  }

  @media (min-width: 768px) {
    .path-card__description {
      font-size: var(--text-base);
    }
  }

  /* Signup Section */
  .signup-section {
    padding: var(--space-8) var(--space-4);
    padding-bottom: var(--space-16);
    padding-bottom: calc(var(--space-16) + var(--safe-area-inset-bottom));
    background-color: var(--color-bg-elevated);
  }

  @media (min-width: 768px) {
    .signup-section {
      padding: var(--space-12) var(--space-8);
      padding-bottom: var(--space-20);
    }
  }

  .signup-wrapper {
    max-width: var(--container-sm);
    margin: 0 auto;
  }
</style>
